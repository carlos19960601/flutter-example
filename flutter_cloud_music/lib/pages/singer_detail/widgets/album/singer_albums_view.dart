import 'package:flutter/material.dart';

class SingerAlbumsView extends StatelessWidget {
  const SingerAlbumsView({super.key, required this.id});

  final int id;

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
